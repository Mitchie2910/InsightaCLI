[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "insighta"
version = "0.1.0"
description = "Insighta CLI tool"
dependencies = ["typer[all]", "httpx", "requests"]

[project.scripts]
insighta = "insighta.main:app"